# =================================================================
# AETHERIUS PROJECT ENVIRONMENT VARIABLES
# =================================================================
# Copy this file to .env and fill in your actual values
# =================================================================

# -----------------------------------------------------------
# GENERAL SETTINGS
# -----------------------------------------------------------
NODE_ENV=development # development | production
LOG_LEVEL=info # error | warn | info | debug

# -----------------------------------------------------------
# GOOGLE GEMINI API
# -----------------------------------------------------------
# Required for Orchestrator and Squad Leader LLM functionality
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# -----------------------------------------------------------
# MONGODB (World State Service)
# -----------------------------------------------------------
# MongoDB connection string
# Local: mongodb://localhost:27017/aetherius_world_state
# Atlas: mongodb+srv://user:pass@cluster.mongodb.net/aetherius_world_state
MONGO_URI=mongodb://localhost:27017/aetherius_world_state

# -----------------------------------------------------------
# WORLD STATE SERVICE
# -----------------------------------------------------------
# HTTP API port
PORT=3000
# WebSocket port for real-time updates
WS_PORT=3001

# -----------------------------------------------------------
# ORCHESTRATOR SERVICE
# -----------------------------------------------------------
# HTTP port (currently placeholder)
ORCHESTRATOR_PORT=5000
# WebSocket port for communication with BSMs and Squad Leaders
ORCHESTRATOR_WS_PORT=5001
# World State Service API address
WORLD_STATE_API_ADDRESS=http://localhost:3000
# Path to Squad Leader script (optional, uses default if not set)
# SQUAD_LEADER_SCRIPT_PATH=../../squad-leader/dist/index.js

# -----------------------------------------------------------
# BOT SERVER MANAGER (BSM)
# -----------------------------------------------------------
# WebSocket server port for Orchestrator/Squad Leader connections
BSM_WS_PORT=4000
# Local TCP port for Bot Agents
BSM_AGENT_PORT=4001
# Orchestrator WebSocket address
ORCHESTRATOR_ADDRESS=ws://localhost:5001
# BSM unique identifier (optional, auto-generated if not set)
# BSM_ID=bsm-01
# Path to Bot Agent script (optional, uses default if not set)
# AGENT_SCRIPT_PATH=../bot-agent/dist/index.js

# -----------------------------------------------------------
# MINECRAFT SERVER CONNECTION
# -----------------------------------------------------------
# Minecraft server hostname or IP
MC_HOST=localhost
# Minecraft server port
MC_PORT=25565
# Minecraft version (must match server version)
MC_VERSION=1.20.1

# -----------------------------------------------------------
# BOT AGENT
# -----------------------------------------------------------
# Agent ID (auto-generated by BSM, don't set manually)
# AGENT_ID=agent-xyz
# BSM TCP port (set by BSM when spawning agent)
# BSM_TCP_PORT=4001
# BSM host address (optional, defaults to localhost)
BSM_HOST=127.0.0.1

# -----------------------------------------------------------
# SQUAD LEADER
# -----------------------------------------------------------
# Squad ID (auto-generated by Orchestrator, don't set manually)
# SQUAD_ID=squad-abc
# Orchestrator address (set by Orchestrator when spawning squad leader)
# ORCHESTRATOR_ADDRESS=ws://localhost:5001

# =================================================================
# ADVANCED SETTINGS (Optional)
# =================================================================

# -----------------------------------------------------------
# RATE LIMITING
# -----------------------------------------------------------
# Maximum Gemini API calls per minute
GEMINI_RATE_LIMIT_CALLS=60
# Gemini rate limit window in milliseconds
GEMINI_RATE_LIMIT_WINDOW=60000

# -----------------------------------------------------------
# CACHING
# -----------------------------------------------------------
# LLM response cache TTL in milliseconds (5 minutes)
LLM_CACHE_TTL=300000
# Maximum cache size
LLM_CACHE_MAX_SIZE=1000

# -----------------------------------------------------------
# CIRCUIT BREAKER
# -----------------------------------------------------------
# Number of failures before circuit opens
CIRCUIT_BREAKER_FAILURE_THRESHOLD=5
# Number of successes needed to close circuit
CIRCUIT_BREAKER_SUCCESS_THRESHOLD=2
# Circuit reset timeout in milliseconds
CIRCUIT_BREAKER_RESET_TIMEOUT=30000

# -----------------------------------------------------------
# PERCEPTION MODULE
# -----------------------------------------------------------
# Perception scan interval in milliseconds
PERCEPTION_INTERVAL=10000
# Perception radius in blocks
PERCEPTION_RADIUS=32

# -----------------------------------------------------------
# RETRY SETTINGS
# -----------------------------------------------------------
# Maximum retry attempts for failed operations
MAX_RETRIES=5
# Base delay for exponential backoff in milliseconds
RETRY_BASE_DELAY=1000
# Maximum delay for retries in milliseconds
RETRY_MAX_DELAY=30000

# =================================================================
# NOTES
# =================================================================
#
# 1. REQUIRED VARIABLES:
#    - GEMINI_API_KEY
#    - MONGO_URI
#    - MC_HOST, MC_PORT, MC_VERSION
#
# 2. Service Startup Order:
#    a) MongoDB (external)
#    b) World State Service
#    c) Bot Server Manager
#    d) Orchestrator Service
#
# 3. The Orchestrator will spawn Squad Leader processes dynamically
# 4. The BSM will spawn Bot Agent processes dynamically
#
# 5. For distributed deployment, adjust addresses to point to actual
#    hostnames/IPs instead of localhost
#
# =================================================================
